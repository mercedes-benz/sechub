{
    "type" : "executorConfigurationList",
    
    "executorConfigurations" : [
    {
        
        "uuid": "c2bc2b12-b8cc-46db-b954-f50d9e000df1",
        
        /* instances are collected by spring boot but are identified by product identifier and version! */
        "productIdentifier" : "PDS_CODESCAN",
        
        "executorVersion" : 1,
        
        "enabled" : true
    },
    {
        
        /* another pds code scan */
        
        "uuid": "d8172c3f-7089-4b77-b5f7-1bb752c25f08",
        
        "productIdentifier" : "PDS_CODESCAN",
        
        "executorVersion" : 1,
        
        "enabled" : true
        
    },
    {
        
        "uuid": "21d9cfe6-a891-482b-847e-863d5e73d3a0",
        
        /* a prodcut example: here no additional job parameters are defind, only credentials, */
        /* so we just keep old way alive (programmatical settings for job data */
        /* later it shall be possible to use job parameters in every adapter so e.g. product adapter */
        /* will be set by checkmarxAdapter.setAgent(jobParameters.get("checkmarx.agent.name") instead */
        /* doing spring boot values + install setup. But for compatibility (avoiding too much changes) */
        /* we keep old things alive */
        "productIdentifier" : "CHECKMARX",
        
        "executorVersion" : 1,
        
        "enabled" : true
    }
    
    
    ]
    
}