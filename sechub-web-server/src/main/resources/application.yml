# SPDX-License-Identifier: MIT

# Main settings
server:
  port:
    4443
  ssl:
    enabled: true # always enabled
    protocol: TLS
    enabled-protocols: TLSv1.2,TLSv1.3

spring:
  messages:
    basename: "i18n/messages"
  profiles:
    group:
      webserver_prod: "ssl-cert-required,oauth2-enabled"
      webserver_int: "ssl-cert-required,oauth2-enabled,classic-auth-enabled"
      webserver_dev: "ssl-cert-provided,classic-auth-enabled"
      webserver_local: "ssl-cert-provided,classic-auth-enabled,local"
      webserver_test: "test"
      webserver_integrationtest: "ssl-cert-provided,integrationtest-data"
  web:
    resources:
      static-locations: classpath:/static
