CREATE TABLE users (
                       user_id varchar(60) not null,
                       user_email_adress varchar(255) not null,
                       user_role VARCHAR(50) NOT NULL,
                       PRIMARY KEY (user_id)
);