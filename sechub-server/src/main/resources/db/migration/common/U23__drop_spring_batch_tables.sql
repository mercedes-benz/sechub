-- SPDX-License-Identifier: MIT

-- We do nothing here. Reason: If a migration to a lower version is necessary, this means also
-- that we have an older server implementation up and running.
-- In this case the (old) server version will automatically create the missing spring batch tables
