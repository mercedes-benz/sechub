# SPDX-License-Identifier: MIT

version: "3"
services:
    pds_gosec:
        build:
            args:
                - BASE_IMAGE=ubuntu:focal
            context: docker/
        env_file:
            - .env
        container_name: pds_gosec
        ports:
            - "8444:8444"
